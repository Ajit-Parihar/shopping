<%@sellerCategory.each do |category|%> 
   <%=category.id%>
   <%= link_to category_product_path(category.id) do %>
      <%= image_tag asset_path(Rails.application.config.images_hash[category.category.to_sym]) %>
   <% end %>
<%end%>


